class Game:
    'Classe general per al joc'
    
    #atributs
    def __init__(self):
    
    def handleEvent(self, event):
        if type(event) is
    
    def draw(self, window):
        # window.draw()
        
